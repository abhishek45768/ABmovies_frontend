// App.jsx
import { BrowserRouter } from 'react-router-dom';
import Layout from './layout/layout';

const App = () => {
    return (
        <BrowserRouter>
            <Layout />
        </BrowserRouter>
    );
};

export default App;
